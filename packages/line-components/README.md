# @websublime/line-components

Headless web component implementations for the line://ui design system. Empty in Phase 00 — the build pipeline is wired and smoke-tested, but no components ship until Phase 1.
