# @websublime/line-themes

Role-mapped theme layers (accent, gray, semantics, aliases, defaults) consumed via CSS subpaths. Depends on `@websublime/line-colors` and `@websublime/line-schemas` (wired in B2).
